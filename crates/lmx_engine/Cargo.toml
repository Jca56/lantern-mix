[package]
name = "lmx_engine"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
publish.workspace = true
description = "The real-time audio graph: decks, channel strips, crossfader, FX, cue bus, master. process() never allocates, locks, blocks, or panics."

[dependencies]
lmx_core = { workspace = true }
lmx_rt = { workspace = true }
lmx_dsp = { workspace = true }
