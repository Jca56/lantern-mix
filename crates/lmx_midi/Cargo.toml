[package]
name = "lmx_midi"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
publish.workspace = true
description = "Raw MIDI over /dev/snd (std I/O only), parser with 14-bit CC pairing, mapping engine (.lmxmap), built-in DDJ-GRV6 profile, LED feedback."

[dependencies]
lmx_core = { workspace = true }
lmx_engine = { workspace = true }
