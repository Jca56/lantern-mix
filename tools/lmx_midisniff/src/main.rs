//! Dump raw MIDI from a /dev/snd device with timestamps; pair 14-bit CCs; --send for LED probing. Phase 0 tool for mapping the DDJ-GRV6.
//!
//! Design: see `docs/` — this crate is a skeleton; responsibilities and module
//! boundaries are decided, logic is not written yet.


fn main() {
    println!("lmx_midisniff — design phase. See docs/.");
}
