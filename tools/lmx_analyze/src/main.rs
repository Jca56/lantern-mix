//! CLI: analyze a file, print tempo candidates, grid, key, loudness; dump ODF/chroma as CSV. For tuning the detectors on real tracks.
//!
//! Design: see `docs/` — this crate is a skeleton; responsibilities and module
//! boundaries are decided, logic is not written yet.


fn main() {
    println!("lmx_analyze — design phase. See docs/.");
}
